# Notes

One markdown file per item, named `<item-id>.md`. The frontend fetches and renders these inline beneath the matching item.

Item IDs are stable SHA1 hashes of (source, canonical URL) — copy the ID from the item card's "add note" link.
